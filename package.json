{
  "name": "test",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build:wasm": "cd wasm-src && rimraf ./wasm-build && wasm-pack build --target web --out-dir ./wasm-build",
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@types/audioworklet": "^0.0.32",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-piano": "^3.1.3",
    "wasm": "file:./wasm-src/wasm-build"
  },
  "devDependencies": {
    "@types/react": "^18.0.17",
    "@types/react-dom": "^18.0.6",
    "@vitejs/plugin-react": "^2.0.1",
    "rimraf": "^3.0.2",
    "typescript": "^4.6.4",
    "vite": "^3.0.7"
  }
}
